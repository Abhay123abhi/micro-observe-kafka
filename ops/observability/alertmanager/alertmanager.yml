global:
  resolve_timeout: 5m

route:
  receiver: incident-platform
  group_by: [alertname, job, service]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h

receivers:
  - name: incident-platform
    webhook_configs:
      - url: http://incident-service:8084/api/incidents/webhooks/alertmanager
        send_resolved: true

inhibit_rules:
  - source_matchers: ['alertname = ServiceUnavailable']
    target_matchers: ['severity =~ warning|critical']
    equal: [job]
